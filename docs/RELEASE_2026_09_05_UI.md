# AIstock スマホ・表示密度の本番更新

2026年9月5日。「スマホのUIもインスタに寄せて、本番どうぞ」の依頼に基づく公開です。

## 公開結果

- 正式URL：<https://mon-ai.jp/aistock>
- 公開ソース：`0db20c51b33ca3e7031dc6fe2fd4dd9c71f2b34c`。GitHub mainと既存Sitesのソースへ送信済み。
- Sites版：24 / `appgprj_6a91470d8eac8191a959f89354b8b900~appgver_e69bd09683088191a19b6b2b81f7be72`
- 公開処理：`appgdep_6a9c2503c4408191b3ec416fdfe29ff7`、`succeeded`（2026-09-05 14:20 UTC）。
- 実行元：<https://toyota-ai-school.mondism.chatgpt.site>。既存の親MON-ai経路を継続し、親Vercelの再デプロイ・設定変更は不要でした。

この公開記録を追加するコミットは文書だけの更新で、上記の実行ソースとは区別します。

## 変更内容

- スマホは薄い上バーとアイコン中心の下ナビ。投稿は画面幅を使い、写真は4:3。名前の読み上げと44px以上の主要操作領域を維持。
- PCも含め前置き・枠・余白を抑え、本文16pxのまま表示密度を調整。背景を操作領域と読む内容で分ける。
- 投稿の種類は開閉式。条件付きURLでは選択内容を開いて表示。「はじめてのAI」は見出し横から開ける。
- 色・押下・表示の短い動きを揃え、対応ブラウザーの同一ドメイン遷移だけ短いフェードを追加。非対応時は通常のリンク。動きを減らす設定も維持。
- バッジ表記は「公式AI」。プロフィールや規約のAIキャラクター説明は残す。

詳細は[AISTOCK_DESIGN.md](AISTOCK_DESIGN.md)を参照。認証・保存・DB・会員データ・課金設定は変更していません。元の作業場所にある`lib/chatgpt-columns.ts`の型追加はそのまま残し、別作業ツリーから公開物を作ることでコミット・配信物から除外しました。

## 検証と境界

- 公開コミットの作業ツリーで`npm run verify`、型検査が成功。全730課題・100コラム・185直接素材を維持。
- 正式URLのホーム、公式AIフィード、種類指定フィード、検索、学ぶ、公式AIプロフィール、教材紹介、教材検索、Lv.137、登録、ログインの11経路がHTTP 200で、新しいCSS参照を含むことを確認。
- 新CSS `index.CJIeFgxl.css`、ロゴ、教材用JPEGがHTTP 200。公開物と配信バイト列のSHA-256が一致。
- 旧`/aijuku/community?view=ai`は同じ経路・クエリの`/aistock/community?view=ai`へ307。
- 未ログインの交流・投稿APIへの空のPOSTは401・no-store。データ変更はありません。交流APIのGETは仕様上405であり、POSTの認証確認と混同しないでください。
- 実機でのブラウザー操作、スクリーンショットによる見た目、FPSや200%拡大の実測は未実施です。HTTP・静的検査・ビルドの結果と区別します。

アプリ内ブラウザーへの表示要求はqueuedで返されました。画面が自動で切り替わった証拠とは扱わず、上記の正式URLから確認できます。
