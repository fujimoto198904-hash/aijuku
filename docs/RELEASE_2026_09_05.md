# AIstock 本番公開記録

2026年9月5日、本人の「本番公開して」に基づき公開しました。

## 公開先と版

- 正式URL：<https://mon-ai.jp/aistock>
- 実行元：既存Sitesプロジェクト `appgprj_6a91470d8eac8191a959f89354b8b900`。D1 `DB` / R2 `MEDIA`を継続。
- アプリ公開ソース：`8a20508630850e05c0830c7ba1a9bddcd98317fe`。この後の公開記録だけのコミットは、実行コードの更新ではありません。
- Sites版：23 / `appgprj_6a91470d8eac8191a959f89354b8b900~appgver_28d4377f6e688191a8acc2c8898e25ef`
- Sites公開：`appgdep_6a9bf4244e788191ac8ff876287244ab`、`succeeded`。
- 親MON-ai：`8c81bb54089f3c58e48552cbd7cb6812ba5eede9`、Vercel `dpl_ENbGapJd8vZXLSc3T5oFnduwZK95`、production / READY。正式ドメインの割当とGit SHAを照合済み。
- 旧`/aijuku`と下層ページは、対応する`/aistock`へ307。クエリも維持。旧`/aijuku/api/`はredirectせず既存Sitesへ接続。

公開用ビルドは無関係な未コミット変更を除外した別作業ツリーで実施。`lib/chatgpt-columns.ts`の既存型追加は原本に残し、今回のコミット・公開物へ含めていません。親サイトは`vercel.json`だけを変更し、本文・画像・ブログを再生成していません。

## データの引き継ぎ

0000〜0018適用済み。0018はINSERTのみで、公式1＋公式AI10プロフィール、公式教材紹介6＋架空投稿例20を導入。公式プロフィールはログイン会員IDを持たず、認証アカウントを作りません。架空AIの人物像・投稿例・写真についての開示を維持しています。

本番D1の読み取りで11プロフィール・26投稿を確認。公開前の既存2会員は全列不変。editorialの規約は適用対象外、同意時刻0です。既存owner/demoに新規約への同意やパスワード変更を代行していません。定期的な自動投稿は未稼働です。

HTTP確認用の非公開会員3件を作成。公開プロフィール・投稿・DMは作成せず、確認セッションはログアウト済み。件数集計から除外します。旧Aitockの本人記録・旧ドメインの一括移転はしていません。

## 検証したこと

- 公開ソースで`npm run verify`成功。全730課題・73章、100コラム、185直接素材ファイルの整合性を維持。型検査も成功。リポジトリ全体のlintが無警告とは表現しません。
- 正式ドメインのホーム・検索・教科書案内・教材検索・Lv.137本文・コラム一覧と詳細3件が200。
- 公式プロフィール、架空AIプロフィール、投稿例詳細、公式教材紹介が200。人物画像は200 / image/jpegで実体を確認。配信JS・CSS・ロゴも200。
- ユーザー名登録 → マイページ → ログアウト → 旧Cookieで交流APIが401 → 再ログインを、本番HTTPで確認。
- 最終版で再ログイン・マイページ・外部Originのlogout拒否403・正規logoutの`/aistock`転送・旧セッション失効を再確認。
- 未ログインの交流APIは401、運営seed APIは403。有料Checkoutは410。
- 元のWebhookはリダイレクトなし。Stripeの本番秘密設定がないため503であり、課金稼働とは扱いません。

Vinextでは、失効Cookieを持つマイページ応答がストリーム開始後の200＋meta refreshになる場合があります。私的な内容を含まず、交流APIも401であることを確認しました。HTTPステータスだけでログアウト失敗と判断しないでください。

## 今後の運用確認

実ブラウザーでの登録操作、任意の復旧コードの本番確認、2人の実会員によるDM・フォロー・公開停止は未確認です。隔離D1/R2での機能検証と区別します。Googleログイン・確認メールは未設定で非表示です。素材は本人提供・サイト使用の依頼に基づき利用し、ライセンス証憑の独立確認済みとは表現しません。

旧学校への復元時は`codex/archive-paid-school-20260905`を参照し、親の経路とSites版を整合させます。現在のD1テーブル・会員・ノート・投稿を削除する巻き戻しはしません。
